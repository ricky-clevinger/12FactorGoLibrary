---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: "1.8"

inputs:
- name: 12FactorGoLibrary

outputs:
- name: built-project

run:
  path: sh
  args:
    - -exc
    - |
      ls -a
      cd 12FactorGoLibrary
      ls -a
      export GOPATH=$PWD
      export PATH=$PATH:$GOPATH
      ls -lat
      go build index.go
      ls -a
