---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: "latest"
    
inputs:
- name: go-library-source

outputs:
- name: built-project

run:
  path: sh
  args:
    - -exc
    - |
      cd go-library-source
      export GOPATH=$PWD
      export PATH=$PATH:$GOPATH
      cd src
      cd 12FactorGoLibrary
      go build index.go
      ls -a
      cd $GOPATH
