---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: "1.8"

inputs:
- name: go-library-source

run:
  path: sh
  args:
    - -exc
    - |
      #go test
