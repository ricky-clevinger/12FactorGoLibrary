---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: "alpine"

inputs:
- name: go-library-source

run:
  path: sh
  args:
    - -exc
    - |
      #go test
