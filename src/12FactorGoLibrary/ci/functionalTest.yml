platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven-selenium
    tag: "latest"

inputs:
- name: built-project
- name: go-library-source

#outputs:
#- name: built-library

run:
  path: sh
  args:
  - -exc
  - |
    cd go-library-source/src/12FactorGoLibrary/ci/LibraryTest
    java -version
    mvn test
